---
phase: quick
plan: 260418-12o
subsystem: wiki-graph
tags: [wiki, graph, context-menu, ux, digest]
key-files:
  modified:
    - src/renderer/src/components/WikiGraph.tsx
    - src/renderer/src/components/WikiPane.tsx
    - src/renderer/src/components/WikiTab.tsx
    - src/renderer/src/components/WikiSidebar.tsx
    - src/main/digestScheduler.ts
decisions:
  - "Graph context menu implemented as internal portal in WikiGraph (not delegated to parent) — keeps mouse position handling local to the component that receives native MouseEvents"
  - "Color picker in WikiGraph uses tagColors prop (controlled value) for live color preview on change"
  - "Weekly digest threshold set to 20h matching daily — generates once per day covering a 7-day window"
metrics:
  duration: "~10 minutes"
  completed: "2026-04-18T04:53:14Z"
  tasks_completed: 3
  files_modified: 5
---

# Quick Task 260418-12o: Fix Wiki Graph Right-Click Context Menu

**One-liner:** Graph right-click now shows a two-item context menu portal (Change tag color / Delete entry) with an inline color picker that dismisses on mouseleave; sidebar color picker also auto-dismisses; weekly digest threshold corrected to 20h.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Graph node right-click — internal context menu with tag awareness | 48b0e91 | WikiGraph.tsx, WikiPane.tsx, WikiTab.tsx |
| 2 | Sidebar color picker — auto-dismiss on mouseleave, remove X button | 1b549f3 | WikiSidebar.tsx |
| 3 | Weekly digest threshold — match daily cadence (20h) | 4622074 | digestScheduler.ts |

## What Was Done

### Task 1: Graph Context Menu

- Added `tag: string` to `GraphNode` interface in WikiGraph.tsx and WikiPane.tsx
- Replaced `onNodeRightClick?: (filename: string) => void` prop with `onNodeDelete` + `onSetTagColor` in both WikiGraph and WikiPane
- Added `ctxMenu` state `{ filename, tag, x, y } | null` with global mousedown dismiss effect
- Added `colorPickerTarget` state `{ tag, x, y } | null` with onMouseLeave dismiss
- `onNodeRightClick` on ForceGraph2D now captures native `event.clientX/clientY` and sets ctxMenu state
- Both portals rendered via `createPortal(document.body)`
- "Delete entry" calls `onNodeDelete(filename)` — triggers existing WikiTab delete confirmation modal
- "Change tag color" opens color picker portal using controlled `value={tagColors[tag]}` with onSetTagColor on change
- WikiTab.tsx: added `tag: f.tags[0] ?? ''` to graphNodes map; replaced `onNodeRightClick` with `onNodeDelete={handleDeleteFile}` and `onSetTagColor={handleSetTagColor}`

### Task 2: Sidebar Color Picker UX

- Added `onMouseLeave={() => setColorPicker(null)}` to the colorPicker portal div in WikiSidebar.tsx
- Removed the `<button onClick={() => setColorPicker(null)}>x</button>` element entirely

### Task 3: Weekly Digest Threshold

- Changed `maybeDispatchDigest('weekly', 168, 144, workerPort)` to `maybeDispatchDigest('weekly', 168, 20, workerPort)`
- Weekly digest now regenerates once per day (same 20h threshold as daily), still covering a 168h (7-day) window

## Deviations from Plan

None — plan executed exactly as written.

## Verification

- `npx tsc --noEmit`: no errors
- `npm run build`: succeeded (built in 2.85s)
- WikiGraph.tsx: no `onNodeRightClick` prop; uses `onNodeDelete` + `onSetTagColor`
- WikiPane.tsx: threads `onNodeDelete` and `onSetTagColor`; no `onNodeRightClick`
- WikiTab.tsx: graphNodes include `tag` field; passes correct props to WikiPane
- WikiSidebar.tsx: color picker portal has `onMouseLeave` dismiss; X button removed
- digestScheduler.ts: weekly threshold is 20 (not 144)

## Self-Check: PASSED

- 48b0e91 exists: FOUND
- 1b549f3 exists: FOUND
- 4622074 exists: FOUND
- All 5 modified files committed
