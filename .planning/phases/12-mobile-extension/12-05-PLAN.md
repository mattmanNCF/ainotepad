---
phase: 12-mobile-extension
plan: "05"
type: execute
wave: 3
depends_on:
  - "12-03"
files_modified:
  - .github/workflows/deploy-mobile.yml
  - mobile-pwa/README.md
autonomous: false
requirements:
  - MOB-PWA-01
user_setup:
  - service: github-pages
    why: "Hosts the mobile PWA at https://mattmanNCF.github.io/notal-mobile/"
    dashboard_config:
      - task: "Enable GitHub Pages and set Source = GitHub Actions (NOT 'Deploy from a branch')"
        location: "github.com/mattmanNCF/ainotepad -> Settings -> Pages -> Build and deployment -> Source"
      - task: "Add repository secret GOOGLE_WEB_CLIENT_ID with the Web client ID from Plan 12-01 user_setup"
        location: "github.com/mattmanNCF/ainotepad -> Settings -> Secrets and variables -> Actions -> New repository secret"

must_haves:
  truths:
    - ".github/workflows/deploy-mobile.yml exists and triggers on push to main when mobile-pwa/** changes"
    - "The workflow uses actions/checkout@v4, actions/setup-node@v4, runs npm ci in mobile-pwa/, sets VITE_GOOGLE_WEB_CLIENT_ID from secrets, runs npm run build"
    - "The workflow uploads docs/notal-mobile/ via actions/upload-pages-artifact@v3 and deploys via actions/deploy-pages@v4"
    - "On first trigger, the PWA is reachable at https://mattmanNCF.github.io/notal-mobile/ and installs to iOS home screen correctly (verified via Plan 12-06 UAT)"
    - "GOOGLE_WEB_CLIENT_ID is set as a GitHub Actions repository secret by Matt"
  artifacts:
    - path: ".github/workflows/deploy-mobile.yml"
      provides: "Pages deployment pipeline"
      contains: "actions/deploy-pages"
    - path: "mobile-pwa/README.md"
      provides: "Local dev + deploy instructions"
      contains: "VITE_GOOGLE_WEB_CLIENT_ID"
  key_links:
    - from: ".github/workflows/deploy-mobile.yml"
      to: "mobile-pwa/ build output at docs/notal-mobile/"
      via: "working-directory + upload-pages-artifact path"
      pattern: "docs/notal-mobile"
    - from: "GitHub Pages hosted URL"
      to: "VitePWA base /notal-mobile/ subpath"
      via: "base config in mobile-pwa/vite.config.ts matches Pages URL segment"
      pattern: "notal-mobile"
---

<objective>
Stand up the GitHub Actions pipeline that builds the mobile PWA (Plan 12-03's mobile-pwa/ project) on push to main and deploys it to GitHub Pages at https://mattmanNCF.github.io/notal-mobile/. Coordinate the two manual setup steps Matt must perform in the GitHub UI (enable Pages with Actions source + add the GOOGLE_WEB_CLIENT_ID secret).

Purpose: MOB-PWA-01 requires the PWA to be hosted on GitHub Pages and installable on iOS/Android. The CI pipeline makes future updates zero-click — push to main, Pages refreshes.

Output: Workflow file, README explaining local dev + deploy mechanics, one live deploy, and Matt confirming the site is reachable.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-mobile-extension/12-RESEARCH.md
@.planning/phases/12-mobile-extension/12-03-SUMMARY.md

<interfaces>
<!-- GitHub Pages CI pattern from 12-RESEARCH.md section "GitHub Actions CI for Pages Deploy": -->

```yaml
name: Deploy Notal Mobile PWA
on:
  push:
    branches: [main]
    paths: ['mobile-pwa/**']
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Build PWA
        working-directory: mobile-pwa
        env:
          VITE_GOOGLE_WEB_CLIENT_ID: ${{ secrets.GOOGLE_WEB_CLIENT_ID }}
        run: npm ci --legacy-peer-deps && npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs/notal-mobile
      - uses: actions/deploy-pages@v4
```

<!-- Current branch state: per STATE.md, working branch is `notal-v0.3.1-mobile` cut from `notal-v0.2`. -->
<!-- For the workflow to actually deploy, Matt needs to merge to main OR we adjust the trigger to the working branch. -->
<!-- Decision: Phase 12 ships when merged to main for v0.3.1 release. Workflow triggers on main. Matt can manually dispatch via workflow_dispatch earlier. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write deploy-mobile.yml + mobile-pwa README</name>
  <files>.github/workflows/deploy-mobile.yml, mobile-pwa/README.md</files>
  <read_first>
    - C:/Users/mflma/workspace/AInotepad/.github/ (confirm whether .github/workflows/ directory exists — if not, create it)
    - C:/Users/mflma/workspace/AInotepad/mobile-pwa/package.json (confirm build script is exactly `tsc --noEmit && vite build`)
    - C:/Users/mflma/workspace/AInotepad/.planning/phases/12-mobile-extension/12-RESEARCH.md (GitHub Actions CI section — use as-is)
  </read_first>
  <action>
1. Create .github/workflows/deploy-mobile.yml with this exact content:

```yaml
name: Deploy Notal Mobile PWA

on:
  push:
    branches: [main]
    paths:
      - 'mobile-pwa/**'
      - '.github/workflows/deploy-mobile.yml'
  workflow_dispatch:

# Cancel in-flight deploys on repush.
concurrency:
  group: pages-mobile
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: mobile-pwa/package-lock.json

      - name: Install deps
        working-directory: mobile-pwa
        run: npm ci --legacy-peer-deps

      - name: Typecheck
        working-directory: mobile-pwa
        run: npm run typecheck

      - name: Build PWA
        working-directory: mobile-pwa
        env:
          VITE_GOOGLE_WEB_CLIENT_ID: ${{ secrets.GOOGLE_WEB_CLIENT_ID }}
        run: npm run build

      - name: Verify manifest subpath
        run: |
          test -f docs/notal-mobile/index.html
          test -f docs/notal-mobile/manifest.webmanifest
          grep -q '/notal-mobile/' docs/notal-mobile/manifest.webmanifest

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/notal-mobile

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

2. Create mobile-pwa/README.md:

```markdown
# Notal Mobile PWA

Static PWA that captures notes on mobile and writes them to Google Drive appDataFolder.
The desktop Notal app polls Drive every 60 seconds and ingests each note via the shared
createNote() code path, then deletes the source file.

## Architecture

See `.planning/phases/12-mobile-extension/12-RESEARCH.md` in the main repo for the full
architecture writeup.

- **Auth**: Google Identity Services (GIS) token model. Access tokens only, no refresh
  tokens in the browser. Re-auth on each session open.
- **Transport**: Drive `appDataFolder` (OAuth-client-private, shared across clients in
  the same GCP project).
- **Offline**: IndexedDB queue via `idb@8`.
- **Hosting**: GitHub Pages at https://mattmanNCF.github.io/notal-mobile/

## Local dev

```bash
cd mobile-pwa
cp .env.example .env.local
# Edit .env.local and paste your Web OAuth client ID from GCP
npm install --legacy-peer-deps
npm run dev
```

The Vite dev server binds to `0.0.0.0:5173` so you can connect from a phone on the same
LAN (`http://<your-mac-ip>:5173/notal-mobile/`). Note: GIS will reject localhost OAuth
redirects — add `http://localhost:5173` and your LAN IP as Authorized JavaScript origins
in the Web OAuth client's GCP settings for local testing.

## Production deploy

Automated via `.github/workflows/deploy-mobile.yml`:

- **Trigger**: push to `main` touching `mobile-pwa/**`.
- **Secret**: `GOOGLE_WEB_CLIENT_ID` must be set in repo Settings -> Secrets -> Actions.
- **GitHub Pages Source**: Settings -> Pages -> Source must be "GitHub Actions" (not
  "Deploy from a branch").

Manual deploy: Settings -> Actions -> Deploy Notal Mobile PWA -> Run workflow.

## Build output

Vite builds to `../docs/notal-mobile/` at the repo root. Contents:
- `index.html` — HTML shell with apple-touch-icon + GIS script tag
- `manifest.webmanifest` — PWA manifest (scope + start_url both `/notal-mobile/`)
- `sw.js` + `workbox-*.js` — service worker (auto-update on deploy)
- `assets/*` — hashed JS/CSS bundles
- `pwa-192x192.png`, `pwa-512x512.png`, `apple-touch-icon-180x180.png`

## iOS home-screen install

1. Open the site in Safari.
2. Share -> Add to Home Screen.
3. Installed PWAs are exempt from the 7-day IndexedDB eviction cap (per WebKit storage
   policy). The queue persists across tab close + reopen.

## Limits

- Per-note text: 16 KiB max (enforced by desktop ajv validation on ingest).
- Drive appDataFolder: warn at 10 MB, hard-stop at 100 MB (stuck-loop protection).
- Access tokens: 1-hour TTL; re-auth required via in-app Connect button.
```

3. Run locally to verify the workflow YAML parses (no CI runner needed — just YAML syntax):
   npx --yes js-yaml .github/workflows/deploy-mobile.yml > /dev/null
   Expect no errors.

4. Commit:
   node ~/.claude/get-shit-done/bin/gsd-tools.cjs commit "ci(12-05): GitHub Pages deploy pipeline for mobile PWA + README" --files .github/workflows/deploy-mobile.yml mobile-pwa/README.md
  </action>
  <verify>
    <automated>test -f .github/workflows/deploy-mobile.yml &amp;&amp; test -f mobile-pwa/README.md &amp;&amp; npx --yes js-yaml .github/workflows/deploy-mobile.yml &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy-mobile.yml exists
    - The YAML parses (no js-yaml error)
    - The workflow file contains "actions/checkout@v4" and "actions/setup-node@v4" and "actions/upload-pages-artifact@v3" and "actions/deploy-pages@v4" — grep each returns 1
    - The workflow contains VITE_GOOGLE_WEB_CLIENT_ID: ${{ secrets.GOOGLE_WEB_CLIENT_ID }} — grep "secrets.GOOGLE_WEB_CLIENT_ID" returns 1
    - The workflow path filter includes 'mobile-pwa/**' — grep "mobile-pwa/\*\*" returns 1
    - The workflow uses path 'docs/notal-mobile' in upload-pages-artifact — grep "path: docs/notal-mobile" returns 1
    - mobile-pwa/README.md exists and contains VITE_GOOGLE_WEB_CLIENT_ID reference and GitHub Pages instructions — grep "VITE_GOOGLE_WEB_CLIENT_ID" and "GitHub Pages" each >= 1
    - git log -1 --format=%s matches ci(12-05): prefix
  </acceptance_criteria>
  <done>Workflow YAML and README committed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Matt enables GitHub Pages + adds secret + triggers first deploy</name>
  <files></files>
  <read_first>
    - C:/Users/mflma/workspace/AInotepad/mobile-pwa/README.md (just-written — walks Matt through the process)
  </read_first>
  <action>Human-only checkpoint. See &lt;what-built&gt;, &lt;how-to-verify&gt;, and &lt;resume-signal&gt; below for the detailed instructions and resume semantics.</action>
  <verify>
    <automated>MISSING — human checkpoint; see &lt;how-to-verify&gt;</automated>
  </verify>
  <done>Resume signal received from user per &lt;resume-signal&gt;.</done>
  <what-built>
Task 1 shipped the workflow YAML. Matt must now perform two one-time setup actions in the GitHub repo UI (these CANNOT be automated — `gh repo edit` does not expose Pages source selection programmatically; secrets must be set by the repo owner). After setup, Matt triggers the first deploy.
  </what-built>
  <how-to-verify>
Matt performs each step in order.

Step 1 — Enable GitHub Pages with Actions source:
  1. Go to https://github.com/mattmanNCF/ainotepad/settings/pages
  2. Under "Build and deployment", set **Source** to **GitHub Actions** (NOT "Deploy from a branch")
  3. Save if prompted

Step 2 — Add the repository secret:
  1. Go to https://github.com/mattmanNCF/ainotepad/settings/secrets/actions
  2. Click "New repository secret"
  3. Name: `GOOGLE_WEB_CLIENT_ID`
  4. Secret value: the Web OAuth client ID Matt created in GCP during Plan 12-01 user_setup (the string ending in `.apps.googleusercontent.com`)
  5. Save

Step 3 — Add Pages origin to the Web OAuth client's Authorized JavaScript origins in GCP:
  1. Go to https://console.cloud.google.com/apis/credentials
  2. Click the Web OAuth client created for Plan 12-01
  3. Under "Authorized JavaScript origins", add: `https://mattmanNCF.github.io`
  4. Save

Step 4 — Trigger the first deploy:
  1. Push the Plan 12-01, 12-02, 12-03, 12-04, 12-05 commits to the working branch `notal-v0.3.1-mobile`.
  2. Merge the branch to `main` (or fast-forward if no conflicts — the working branch was cut from notal-v0.2 per STATE.md, so rebase onto main first if main has diverged).
  3. The push to main with mobile-pwa/** changes auto-triggers the workflow.
  4. Alternative: go to https://github.com/mattmanNCF/ainotepad/actions/workflows/deploy-mobile.yml -> "Run workflow" -> select main -> Run.

Step 5 — Verify the deploy succeeded:
  1. Watch the workflow run on the Actions tab.
  2. When complete, open https://mattmanNCF.github.io/notal-mobile/ in a desktop browser.
  3. Confirm the page loads and shows the "Notal Capture" UI.
  4. Open DevTools -> Application tab -> Manifest. Confirm scope and start_url both `/notal-mobile/`.
  5. Click "Connect Google". A popup should open with the Google consent screen showing scope `See, create, and delete its own configuration data in your Google Drive`.

Step 6 — Approve if all succeeded.
  </how-to-verify>
  <resume-signal>
Type "approved" once the PWA is reachable at https://mattmanNCF.github.io/notal-mobile/ and the GIS connect button triggers the consent prompt. Type "failed: {what broke}" if the deploy failed or the site is unreachable after 5 minutes.
  </resume-signal>
</task>

</tasks>

<verification>
- Workflow YAML committed
- README committed
- Matt has enabled Pages with Actions source
- Matt has set GOOGLE_WEB_CLIENT_ID secret
- Matt has added https://mattmanNCF.github.io to the Web OAuth client's authorized origins in GCP
- First deploy completed successfully
- PWA is live at https://mattmanNCF.github.io/notal-mobile/
</verification>

<success_criteria>
- PWA hosted on GitHub Pages (MOB-PWA-01 complete)
- Subsequent pushes to main that touch mobile-pwa/ automatically redeploy
- Manifest verified correct (scope + start_url)
- GIS consent flow initiates successfully from the live URL
</success_criteria>

<output>
After completion, create .planning/phases/12-mobile-extension/12-05-SUMMARY.md with:
- Live URL
- First-deploy workflow run URL (https://github.com/mattmanNCF/ainotepad/actions/runs/...)
- Manifest contents verified (paste the manifest.webmanifest body)
- Any deviations (e.g. if Matt had to add Pages origin under a different setting)
</output>
