---
phase: 11-google-calendar-integration
plan: 07
type: execute
wave: 5
depends_on: [11-05, 11-06]
files_modified: []
autonomous: false
requirements:
  - CAL-SEC-01
  - CAL-SEC-02
  - CAL-SEC-03
  - CAL-UX-01
  - CAL-UX-02
  - CAL-TZ-01
  - CAL-COST-01
  - CAL-DEL-01
  - XCUT-SEC-02
  - XCUT-CSP-01
must_haves:
  truths:
    - "Human operator confirms the v0.3.1 ship-gate end-to-end flow on a packaged NSIS installer (not dev mode) — matches advisor guidance: ship gate = binary verify at the boundary"
    - "Automated security check: asar extract grep for GOCSPX- returns zero hits on the packaged build"
    - "Automated security check: config.json and calendar-settings.json scanned for Google token prefixes (ya29., 1//, GOCSPX-) return zero hits"
    - "CAL-TZ-01 5-zone test matrix verified by re-running npm run test:reminder AND the CI log copied into SUMMARY"
    - "CAL-COST-01 50-note fixture test passes with ≤6 events.insert attempts over 5 true positives"
    - "Every v0.3.1 calendar requirement (CAL-SEC-01/02/03, CAL-UX-01/02, CAL-TZ-01, CAL-COST-01, CAL-DEL-01) has a passing line in the checkpoint transcript"
    - "Every v0.3.1 cross-cutting security requirement (XCUT-SEC-02, XCUT-CSP-01) has a passing line in the checkpoint transcript"
    - "Ship-gate decision recorded: v0.3.1 goes OR gaps file opened and v0.3.1 blocked until closed"
  artifacts:
    - path: ".planning/phases/11-google-calendar-integration/11-07-SUMMARY.md"
      provides: "Human-verified transcript of ship-gate flows with GO/NO-GO decision"
      contains: "SHIP_GATE:"
  key_links: []
---

<objective>
Binary ship-gate for v0.3.1. No code changes — this plan is a single `checkpoint:human-verify` task that walks the user through every Phase 11 acceptance criterion on a freshly built NSIS installer (matches advisor guidance: "ship gate = binary verify at the boundary, not embedded in a feature plan"). Either every row in the checklist passes and v0.3.1 is cleared to ship, or a gap file is opened documenting which requirements failed and v0.3.1 is blocked until closure.

Purpose: Phase 11 is the v0.3.1 ship-gate phase (ROADMAP line 17 + advisor trap #4 "D1 binary gate"). Automated tests and dev-mode smokes in Plans 11-01 through 11-06 are necessary but not sufficient — some requirements (asar secret check, safeStorage round-trip across reinstall, DPAPI mismatch resilience) ONLY manifest correctly on a packaged build. This plan is the forcing function that proves those.

Output: Human-authored transcript in 11-07-SUMMARY.md with PASS/FAIL for each of the 10 Phase 11 requirements + one final SHIP_GATE line (SHIP_GATE: GO or SHIP_GATE: BLOCKED).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-google-calendar-integration/11-RESEARCH.md
@.planning/phases/11-google-calendar-integration/11-01-SUMMARY.md
@.planning/phases/11-google-calendar-integration/11-02-SUMMARY.md
@.planning/phases/11-google-calendar-integration/11-03-SUMMARY.md
@.planning/phases/11-google-calendar-integration/11-04-SUMMARY.md
@.planning/phases/11-google-calendar-integration/11-05-SUMMARY.md
@.planning/phases/11-google-calendar-integration/11-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Ship-gate: v0.3.1 Phase 11 end-to-end verification on packaged installer</name>
  <what-built>
    The full Google Calendar integration described in Phase 11 requirements — OAuth loopback+PKCE, safeStorage-encrypted refresh token, piggybacked reminder detection (zero extra model calls), timezone triple conversion, 10s undo window (or 5s confirm), chip UI, delete cascade, health indicator, full CSP hardening, sandbox:true migration.

    Automated tests that MUST be re-run fresh by the operator as part of this checkpoint (record the console output into 11-07-SUMMARY.md):
    - `npm run typecheck` — must exit 0
    - `npm run test:reminder` — must exit 0 (5-zone timezone matrix + DST + error cases)
    - `npm run build` — must succeed
    - Package the installer: `npm run build:win` (or the existing NSIS build script — see Phase 06 SUMMARY). Record installer size + artifact path.

    Human operator must then install the NSIS, uninstall prior Notal versions first, and execute the checklist below against the installed app.
  </what-built>
  <how-to-verify>
    Prerequisites the operator prepares before starting:
    1. `.env.local` populated with a real Google Desktop-app OAuth2 client ID + secret (Plan 11-01 Task 1 user_setup block lists the dashboard steps)
    2. `npm run build:win` has produced a fresh installer at the path Phase 06 configured (typically `dist/notal-0.3.1-setup.exe` or similar)
    3. A "clean" test Google account is available (to avoid polluting the operator's primary calendar)

    --- AUTOMATED SHIP-GATE CHECKS (run these first; if any fail, skip to BLOCKED) ---

    Check A — CAL-SEC-01 production asar grep:
    ```
    cd C:/Users/mflma/workspace/AInotepad
    mkdir -p out-extracted
    npx @electron/asar extract dist/win-unpacked/resources/app.asar out-extracted
    grep -rE "GOCSPX-[A-Za-z0-9_-]{20,}" out-extracted | tee asar-grep-output.txt
    ```
    EXPECTED: grep output is empty (zero matches). Paste `asar-grep-output.txt` contents into SUMMARY.

    Check B — CAL-SEC-02 config grep (runs AFTER the operator connects in step 3 below):
    ```
    grep -rE "ya29\.|1//|GOCSPX-" "$env:APPDATA/notal"
    ```
    EXPECTED: zero matches. The refresh token is only inside `refreshTokenEncrypted` (base64 of safeStorage output — not a plaintext Google prefix).

    Check C — Re-run timezone test matrix:
    ```
    npm run test:reminder
    ```
    EXPECTED: exit 0, 8/8 tests pass. Paste the tail of the output into SUMMARY.

    --- MANUAL SHIP-GATE CHECKLIST ---

    Launch the installed Notal.exe. DO NOT use `npm run dev` for any of the following.

    1. XCUT-SEC-02 (boot-time assertion present): Does the app launch and stay running? (If boot assertion saw insecure prefs it would exit — if it's running, this passes.) Open DevTools via F12 → Elements → confirm `sandbox=true` on the BrowserWindow by running in console `await window.api.notes.getAll()` returns an array without any `nodeIntegration` errors. [PASS/FAIL]

    2. XCUT-CSP-01 (CSP visibly enforced): DevTools → Sources → inspect index.html → CSP meta tag enumerates at least the 12 connect-src hosts from Plan 11-01. Try a denied connect from console: `fetch('https://example.com')` → EXPECT CSP error in console. [PASS/FAIL]

    3. CAL-SEC-03 + CAL-SEC-01 (OAuth ephemeral loopback + no persisted client_secret): Open Settings → Integrations → click "Connect Google Calendar". Observe:
       - System browser opens a Google consent page
       - The redirect_uri in the URL starts with `http://127.0.0.1:` (NOT `http://localhost:`) and has a non-default port
       - Consent completes and the Notal toast does not appear (we are still not inside the reminder flow — connection only)
       - Settings now shows green dot + "last sync never" + [Disconnect & revoke] button
       - Close app → relaunch → connection persists across restart
       - Run Check A asar grep again (above) — confirm zero matches post-connect
       - Run Check B config grep — confirm zero Google prefix matches
       [PASS/FAIL]

    4. CAL-COST-01 + CAL-TZ-01 + CAL-UX-01 (reminder detection + piggyback + timezone + undo):
       a. Submit a note: "remind me to pick up the dog from the vet tomorrow at 3pm" (note: include a clear date-intent verb)
       b. Within a few seconds a bottom-right toast appears: "Calendar event (undo)" with the event title, human-readable date, and an [Undo] button. A shrinking progress bar runs for 10s.
       c. EXPECTED: no duplicate toasts, no extra model calls (observe Notal's AI provider request rate — should be 1 per note, unchanged from v0.3.0)
       d. Press [Undo] within the 10s window. The toast disappears. The NoteCard footer does NOT show a "▸ Cal" chip. Check calendar.google.com → no event created. [PASS/FAIL UNDO]
       e. Submit a second similar note. Let 10s elapse. Toast flashes ✓ briefly. NoteCard shows blue "▸ Cal" chip. Click the chip → default browser opens the Google Calendar event page.
       f. Confirm the event's date/time matches the user's wall clock (system timezone). [PASS/FAIL AUTO-COMMIT]

    5. CAL-COST-01 50-note corpus test: Submit 50 notes at a steady pace (once per 5 seconds to avoid rate limits). 5 should contain obvious date-intent phrases ("meeting Friday at 2pm", "doctor appt next Wednesday 10am", etc.) and 45 should be casual notes about topics with no reminder ("thoughts on cognitive load", "book recommendation: …", etc.). After all 50 process, query reminders table:
       ```
       sqlite3 "$env:APPDATA/notal/ainotepad.db" "SELECT calendar_sync_status, COUNT(*) FROM reminders GROUP BY calendar_sync_status"
       ```
       EXPECTED: at most 6 rows total (across all statuses), of which the synced count reflects the 5 true positives. Paste the query result into SUMMARY. [PASS/FAIL — must be ≤ 6 total]

    6. CAL-UX-02 health indicator: After step 5, Settings → Integrations shows green dot + "last sync Xm ago" (where X ≤ minutes since the last auto-commit). Disconnect → green becomes red. Reconnect → back to green. [PASS/FAIL]

    7. CAL-TZ-01 manual timezone override test (beyond the automated npm run test:reminder):
       a. Temporarily change the Windows system timezone to "Asia/Kolkata" via Settings → Date & Time
       b. Submit a note "remind me to call my family tomorrow at 10am"
       c. Wait 10s → check the Google Calendar event on calendar.google.com → event time shown in Kolkata's local time (10am IST); in the Google web UI timezone picker, the event time should also be correctly displayed in UTC (4:30 AM UTC).
       d. Restore system timezone.
       [PASS/FAIL — OPTIONAL; may skip if timezone change is disruptive, but record as SKIPPED]

    8. CAL-DEL-01 delete cascade:
       a. Right-click one of the synced-chip notes → Delete
       b. Since a calendar event exists for this note, the "Delete note and calendar event?" confirm dialog MUST appear
       c. Click Delete both. Confirm the event is gone from calendar.google.com (refresh the page).
       d. Delete another synced-chip note, this time checking "Don't ask again" — the cascade happens without a subsequent dialog
       e. Delete a third — this one should NOT prompt (don't-ask-again set). Event still deleted from Google.
       f. Orphan cascade: manually create an event in calendar.google.com with extendedProperties.private.notal_note_id = "fake-orphan-test" (via Google Calendar API Explorer OR manually in the Notal SQLite DB create a note row with id="fake-orphan-test" then delete it). Verify the event is also deleted.
       [PASS/FAIL]

    9. Reinstall resilience: Uninstall Notal. Reinstall the same installer. Launch. Open Settings → Integrations.
       EXPECTED: "not connected" (safeStorage's DPAPI-encrypted token cannot be decrypted across install because Windows assigns new DPAPI keys to new installs — this is correct and secure behavior). No crash. Reconnect works.
       [PASS/FAIL]

    10. CAL-SEC-02 post-flow config grep (runs AFTER all of the above):
        ```
        grep -rE "ya29\.|1//|GOCSPX-" "$env:APPDATA/notal"
        ```
        EXPECTED: zero matches. Refresh token never written in plaintext to any config file.
        [PASS/FAIL]

    --- RECORD THE DECISION ---

    In 11-07-SUMMARY.md, record a table:

    | # | Requirement | Result |
    |---|-------------|--------|
    | 1 | XCUT-SEC-02 | PASS/FAIL |
    | 2 | XCUT-CSP-01 | PASS/FAIL |
    | 3 | CAL-SEC-01 + CAL-SEC-03 | PASS/FAIL |
    | 3 | CAL-SEC-02 (before connect) | PASS |
    | 4 | CAL-UX-01 undo + auto-commit | PASS/FAIL |
    | 5 | CAL-COST-01 ≤6/50 | PASS/FAIL |
    | 6 | CAL-UX-02 health | PASS/FAIL |
    | 7 | CAL-TZ-01 manual (beyond test matrix) | PASS/FAIL/SKIPPED |
    | 8 | CAL-DEL-01 cascade | PASS/FAIL |
    | 9 | DPAPI reinstall behavior | PASS/FAIL |
    | 10 | CAL-SEC-02 (after connect + all ops) | PASS/FAIL |

    Then a single line:

    `SHIP_GATE: GO`  (if ALL rows pass or 7 is SKIPPED with justification)
    OR
    `SHIP_GATE: BLOCKED — see gaps file`  (if any row fails — follow with a gaps file spec pointing to /gsd:plan-phase 11 --gaps)
  </how-to-verify>
  <resume-signal>
    Type "approved — SHIP_GATE: GO" to unblock v0.3.1 release.
    Type "blocked: &lt;reason&gt;" to record BLOCKED and open a gaps file.
  </resume-signal>
</task>

</tasks>

<verification>
- All 10 checklist rows have a recorded PASS/FAIL/SKIPPED result in 11-07-SUMMARY.md
- asar-grep-output.txt is included verbatim in SUMMARY (proves CAL-SEC-01 on the packaged build)
- config-grep output (twice — before connect and after all ops) is included verbatim (proves CAL-SEC-02)
- test:reminder output tail is included (proves CAL-TZ-01 automated slice)
- SHIP_GATE line present exactly once with GO or BLOCKED
- If BLOCKED: a follow-up command is recorded for the operator — `/gsd:plan-phase 11 --gaps` with a list of failing requirement IDs
</verification>

<success_criteria>
- Every Phase 11 requirement (CAL-SEC-01, CAL-SEC-02, CAL-SEC-03, CAL-UX-01, CAL-UX-02, CAL-TZ-01, CAL-COST-01, CAL-DEL-01, XCUT-SEC-02, XCUT-CSP-01) is visibly exercised in the transcript with a PASS
- Decision is binary: GO or BLOCKED, no ambiguity
- Automated ship-gate greps (asar for GOCSPX-, config for Google token prefixes) return zero — written evidence in SUMMARY
- v0.3.1 release tag is only cut AFTER `SHIP_GATE: GO` appears in 11-07-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/11-google-calendar-integration/11-07-SUMMARY.md` containing:
- Automated check outputs (asar grep, config greps, test:reminder tail)
- The 10-row PASS/FAIL table
- Final `SHIP_GATE: GO` or `SHIP_GATE: BLOCKED` line
- If BLOCKED: the gaps-file call to action (`/gsd:plan-phase 11 --gaps`)
- Environment tested: Notal version, installer artifact name + size, OS, test Google account
- Optional narrative: any UX rough edges noticed (not blocking but worth a v0.3.2 polish note)
</output>
