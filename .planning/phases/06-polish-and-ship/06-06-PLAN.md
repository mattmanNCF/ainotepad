---
phase: 06-polish-and-ship
plan: 06
type: execute
wave: 5
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified: []
autonomous: false
requirements:
  - SHIP-01-identity
  - SHIP-02-icon
  - SHIP-03-onboarding
  - SHIP-04-readme
must_haves:
  truths:
    - "npm run build:win succeeds and produces dist/Notal-0.1.0-setup.exe"
    - "npm run build:win produces dist/Notal-0.1.0-win-portable.zip"
    - "App window title shows Notal (not AInotepad)"
    - "First launch shows OnboardingModal"
    - "Second launch skips the modal"
    - "README.md covers all 6 sections under 100 lines"
  artifacts:
    - path: "dist/Notal-0.1.0-setup.exe"
      provides: "Windows NSIS installer"
      min_lines: 1
    - path: "dist/Notal-0.1.0-win-portable.zip"
      provides: "Windows portable zip"
      min_lines: 1
  key_links:
    - from: "electron-builder.yml"
      to: "dist/Notal-0.1.0-setup.exe"
      via: "nsis.artifactName ${productName}-${version}-setup.${ext}"
      pattern: "Notal-0.1.0-setup.exe"
    - from: "electron-builder.yml"
      to: "dist/Notal-0.1.0-win-portable.zip"
      via: "zip.artifactName ${productName}-${version}-win-portable.${ext}"
      pattern: "Notal-0.1.0-win-portable.zip"
---

<objective>
Final verification: build Windows artifacts and perform human smoke test of onboarding flow.

Purpose: Confirm that all Phase 06 changes ship correctly before publishing to GitHub Releases.
Output: Human confirmation that the app builds, installs, and presents the correct first-launch experience.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and verify artifacts</name>
  <files></files>
  <read_first>
    - /c/Users/mflma/workspace/AInotepad/electron-builder.yml
    - /c/Users/mflma/workspace/AInotepad/package.json
  </read_first>
  <action>
Run the full Windows build:
```bash
cd /c/Users/mflma/workspace/AInotepad && npm run build:win
```

**This takes 5-15 minutes — do not interrupt.** electron-builder must compile TypeScript, bundle via electron-vite, then pack two targets (NSIS + zip). Both native modules are already rebuilt from postinstall; the build itself is the slow step.

This runs: `npm run typecheck` → `electron-vite build` → `electron-builder --win`.

Expected output in `dist/`:
- `Notal-0.1.0-setup.exe` — NSIS installer (~80-150MB)
- `Notal-0.1.0-win-portable.zip` — portable zip (~80-150MB)
- `builder-debug.yml` — electron-builder metadata (expected)
- `builder-effective-config.yaml` — effective config (expected)

If the build fails:
1. TypeScript errors → fix in the relevant source file, re-run
2. electron-builder NSIS error → check that nsis.artifactName uses `${productName}` not `${name}`
3. Native module rebuild failure → the project has `npmRebuild: false`; postinstall handles it; if needed run `npm run postinstall` first then re-run build:win
4. Icon file not found → verify `build/icon.ico` exists (from Plan 06-02)
5. Artifact filename shows "ainotepad" instead of "Notal" → check electron-builder.yml productName and artifactName use `${productName}` not `${name}`

Verify artifacts exist after the build completes:
```bash
ls -la /c/Users/mflma/workspace/AInotepad/dist/Notal-0.1.0-setup.exe
ls -la /c/Users/mflma/workspace/AInotepad/dist/Notal-0.1.0-win-portable.zip
```
  </action>
  <verify>
    <automated>
      ls /c/Users/mflma/workspace/AInotepad/dist/Notal-0.1.0-setup.exe &amp;&amp;
      ls /c/Users/mflma/workspace/AInotepad/dist/Notal-0.1.0-win-portable.zip
    </automated>
  </verify>
  <done>
    Both `dist/Notal-0.1.0-setup.exe` and `dist/Notal-0.1.0-win-portable.zip` exist with non-zero sizes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — onboarding + app identity</name>
  <what-built>
    Complete Phase 06 implementation:
    - App renamed to Notal (package.json, electron-builder.yml)
    - Custom geometric notepad icon in build/
    - OnboardingModal shown on first launch (2 steps: welcome + provider setup)
    - onboardingDone flag prevents re-showing the modal
    - SettingsPanel MCP snippet updated to use "notal"
    - Windows NSIS installer and portable zip both built
    - README.md written
  </what-built>
  <how-to-verify>
    **Test 1 — First launch (onboarding modal):**
    1. Run `npm run dev` (or install from dist/Notal-0.1.0-setup.exe)
    2. App should show the OnboardingModal automatically (no click required)
    3. Verify: modal headline says "Welcome to Notal"
    4. Click "Set up provider →" — verify Step 2 shows provider options with Ollama first and "recommended" badge
    5. Click "×" or "Skip" — modal closes, app is fully usable

    **Test 2 — Second launch (no modal):**
    1. Close and re-open the app
    2. Verify: no modal appears on second launch
    3. If modal appears again, the onboardingDone flag was not saved — check ipc.ts onboarding:complete handler

    **Test 3 — App identity:**
    1. Check window title — should say "Notal" (not "AInotepad" or "y")
    2. Check system tray tooltip (if visible) — should say "Notal"
    3. Open Settings → scroll to Agent API section → verify the JSON snippet shows `"notal"` not `"ainotepad"`

    **Test 4 — Build artifacts:**
    1. Check `dist/` folder for `Notal-0.1.0-setup.exe` and `Notal-0.1.0-win-portable.zip`
    2. Verify filenames contain "Notal" not "ainotepad"

    **Test 5 — README:**
    1. Open README.md
    2. Verify it has all 6 sections: concept, download, quick start, provider setup, MCP connection, license
    3. Verify MCP JSON uses "notal" as the server key
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found (e.g., "modal not showing", "wrong artifact name")</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
- OnboardingModal appears on first launch and not on second
- App identity shows Notal throughout (window title, tray, SettingsPanel MCP snippet)
- Both installer and portable zip in dist/ with correct Notal-prefixed filenames
- README covers all 6 sections
</verification>

<success_criteria>
- `dist/Notal-0.1.0-setup.exe` exists
- `dist/Notal-0.1.0-win-portable.zip` exists
- Human confirms first-launch modal, correct identity, README completeness
- Phase 06 marked complete in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-and-ship/06-06-SUMMARY.md`

Update STATE.md:
- current_phase: complete
- status: complete
- Phase 06 row: mark as complete

Phase 06 is complete. Notal v0.1.0 is ready for GitHub Releases upload.
</output>
