---
phase: 06-polish-and-ship
plan: 05
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-04
files_modified:
  - README.md
autonomous: true
requirements:
  - SHIP-04-readme
must_haves:
  truths:
    - "README.md exists and is under 100 lines"
    - "README covers concept, download, quick start, provider setup, MCP connection, and MIT license"
    - "MCP JSON snippet uses notal as the server key (consistent with SettingsPanel)"
    - "README mentions port 7723 and lists all 4 available MCP tools by their exact registered names"
  artifacts:
    - path: "README.md"
      provides: "Developer-focused project documentation"
      contains: "notal"
  key_links:
    - from: "README.md"
      to: "GitHub Releases"
      via: "Download link to latest release"
      pattern: "github.com.*releases"
    - from: "README.md"
      to: "MCP server at 127.0.0.1:7723"
      via: "Agent API section with JSON config"
      pattern: "127.0.0.1:7723"
---

<objective>
Write README.md — developer-focused, under 100 lines, covering the 6 required sections.

Purpose: Primary trust and discoverability artifact for Notal v0.1.0. Developers reading the README must understand what it is, how to install it, how to configure AI, and how to connect agents — without marketing language.
Output: README.md at project root.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <read_first>
    - /c/Users/mflma/workspace/AInotepad/.planning/STATE.md
    - /c/Users/mflma/workspace/AInotepad/.planning/ROADMAP.md
    - /c/Users/mflma/workspace/AInotepad/.planning/phases/06-polish-and-ship/06-CONTEXT.md
    - /c/Users/mflma/workspace/AInotepad/src/main/mcpServer.ts
    - /c/Users/mflma/workspace/AInotepad/package.json
  </read_first>
  <action>
**FIRST: Read `src/main/mcpServer.ts` and extract the exact registered tool names** (the string argument to `server.tool('TOOL_NAME', ...)` calls). The plan provides the expected names below, but verify them from the source file before writing. The README tool list MUST use the actual registered names from mcpServer.ts, not assumed names.

Expected tool names (verify from mcpServer.ts):
- `search_notes`
- `get_note`
- `list_wiki_pages`
- `read_wiki_page`

If the actual names differ, use the names from the file.

**SECOND: Read `package.json` and copy the `homepage` field URL** — use it as the base GitHub URL in the README (currently `https://github.com/mflma/ainotepad`).

Write `README.md` at the project root. The file MUST be under 100 lines total (including blank lines).

Required 6 sections (in order):
1. What it is — one-paragraph concept
2. Download — link to GitHub Releases
3. Quick start — install, launch, open Settings, enter API key
4. Provider setup — Claude, OpenAI, Ollama (brief per-provider instructions)
5. MCP agent connection — exact JSON snippet, list all 4 tools by their verified names
6. License — MIT, link to GitHub

Content constraints:
- No marketing language ("powerful", "revolutionary", "seamlessly")
- Code blocks for all commands and JSON
- MCP JSON snippet must use `"notal"` as the server key (matches SettingsPanel.tsx)
- Port 7723 must appear in the MCP section
- GitHub URL from package.json homepage field
- MIT license stated explicitly

Write the file with this content (substituting verified tool names and homepage URL):

```markdown
# Notal

Write a note. AI organizes it. Your wiki grows automatically.

No prompts, no chatting. Every note is processed silently: tagged, annotated, and woven into a personal knowledge base. Built on Electron with local-first SQLite storage.

## Download

[Latest release →](https://github.com/mflma/ainotepad/releases/latest) — Windows installer (`.exe`) or portable zip.

## Quick start

1. Download and run the installer
2. Press `Ctrl+Shift+N` to capture a note from anywhere
3. Open **Settings** (gear icon, top right) to configure an AI provider
4. Write a note — AI processes it in the background

## Provider setup

**Ollama (free, local)** — install [Ollama](https://ollama.com), pull a model, select it in Settings.
```bash
ollama pull gemma4:e4b
```

**Claude** — get an API key from [console.anthropic.com](https://console.anthropic.com), paste into Settings.

**OpenAI** — get an API key from [platform.openai.com](https://platform.openai.com), paste into Settings.

Other supported providers: Gemini, OpenRouter, Groq, Hugging Face.

## MCP agent connection

Notal runs a local MCP server at `http://127.0.0.1:7723/mcp`.

Add to Claude Code or any MCP-compatible agent:
```json
{
  "mcpServers": {
    "notal": {
      "type": "http",
      "url": "http://127.0.0.1:7723/mcp"
    }
  }
}
```

Available tools:
- `search_notes` — full-text search across all notes
- `get_note` — retrieve a single note by ID
- `list_wiki_pages` — list all wiki page filenames
- `read_wiki_page` — read a wiki page by filename

## License

MIT — see [LICENSE](LICENSE).
```

After writing, verify line count: the file must be 100 lines or fewer.
  </action>
  <verify>
    <automated>
      test -f /c/Users/mflma/workspace/AInotepad/README.md &amp;&amp;
      grep '"notal"' /c/Users/mflma/workspace/AInotepad/README.md &amp;&amp;
      grep '127.0.0.1:7723' /c/Users/mflma/workspace/AInotepad/README.md &amp;&amp;
      grep 'MIT' /c/Users/mflma/workspace/AInotepad/README.md
    </automated>
  </verify>
  <done>
    README.md exists at project root. Contains all 6 sections. Uses "notal" as MCP server key. Port 7723 present. All 4 tool names listed using the exact names from mcpServer.ts. MIT license stated. Line count is 100 or fewer.
  </done>
</task>

</tasks>

<verification>
- `test -f README.md` exits 0
- `grep '"notal"' README.md` exits 0
- `grep '127.0.0.1:7723' README.md` exits 0
- Tool names in README match names registered in `src/main/mcpServer.ts` (verify with grep)
- `grep 'MIT' README.md` exits 0
- Line count via `wc -l README.md` is ≤ 100
</verification>

<success_criteria>
- README.md exists with all 6 required sections
- MCP JSON snippet uses "notal" as server key, consistent with SettingsPanel.tsx
- All MCP tool names in README match the exact registered names from mcpServer.ts
- File is 100 lines or fewer
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-and-ship/06-05-SUMMARY.md`
</output>
