---
phase: 09-app-icon
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - dist/Notal-0.1.0-setup.exe
  - dist/Notal-0.1.0-win-portable.zip
autonomous: false
requirements:
  - ICON-01

must_haves:
  truths:
    - "`npm run build` completes successfully with the new icon pipeline (typecheck + electron-vite build both exit 0)"
    - "`electron-builder --win --dir` produces an unpacked app where notal.exe embeds the illustrated ICO (verifiable via PowerShell ExtractAssociatedIcon)"
    - "The NSIS installer .exe in dist/ shows the illustrated icon in Windows Explorer"
    - "Running `npm run dev` launches the app with the lemur icon visible in the taskbar and system tray (human-verified)"
  artifacts:
    - path: "dist/win-unpacked/notal.exe"
      provides: "Compiled Windows executable with embedded ICO resources"
      min_bytes: 50000000
    - path: "dist/Notal-0.1.0-setup.exe"
      provides: "NSIS installer with illustrated icon on the setup .exe, uninstaller .exe, and installer wizard header"
      min_bytes: 100000000
  key_links:
    - from: "build/icon.ico"
      to: "dist/win-unpacked/notal.exe (icon resource)"
      via: "electron-builder rcedit embeds win.icon into PE file during packaging"
      pattern: "notal\\.exe"
    - from: "resources/tray-icon.png"
      to: "dist/win-unpacked/resources/app.asar.unpacked/resources/tray-icon.png"
      via: "asarUnpack: resources/** in electron-builder.yml"
      pattern: "tray-icon\\.png"
---

<objective>
Build the full Windows distribution with the new icon pipeline and verify — both automatically (via file presence, PE icon extraction) and via a human checkpoint on the running app — that the illustrated lemur icon appears in the taskbar, tray, installer .exe, and installed app shortcut.

Purpose: Plans 01 and 02 produce and wire the assets, but only a real build + real run can confirm Phase 09 Success Criteria 1 ("taskbar and system tray shows the custom illustrated asset") and 2 ("NSIS installer uses the custom icon") are actually met. Code-level greps can't see whether Windows Explorer renders the icon correctly.

Output: A fresh `dist/Notal-0.1.0-setup.exe` and `dist/win-unpacked/notal.exe` that visibly bear the illustrated icon, plus a human-verification checkpoint confirming taskbar/tray rendering on Matt's machine.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-app-icon/09-01-PLAN.md
@.planning/phases/09-app-icon/09-02-PLAN.md
@electron-builder.yml

<interfaces>
<!-- Inputs from plan 01 + 02 (present at plan 03 start) -->
- build/icon.ico — multi-resolution ICO (16/32/48/256)
- resources/icon.png — 512x512 illustrated
- resources/tray-icon.png — 32x32 illustrated
- src/main/index.ts — wired to load icons from disk
- electron-builder.yml — explicit win.icon, nsis.installerIcon, nsis.uninstallerIcon, nsis.installerHeaderIcon

<!-- Build commands available (from package.json) -->
- npm run build — typecheck + electron-vite build (no packaging)
- npm run build:unpack — above + electron-builder --dir (unpacked app, fast, no installer)
- npm run build:win — above + electron-builder --win (full NSIS installer + ZIP)

<!-- Known Windows build constraint from STATE.md decisions -->
- winCodeSign symlink workaround: patch-wincosign.cjs is in postinstall. Should Just Work, but if packaging fails on "symlink / unsupported operation", re-run `node scripts/patch-wincosign.cjs` manually.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the full Windows distribution with the new icon pipeline</name>
  <files>dist/Notal-0.1.0-setup.exe, dist/Notal-0.1.0-win-portable.zip, dist/win-unpacked/notal.exe</files>
  <read_first>
    - C:/Users/mflma/workspace/AInotepad/package.json (confirm build:win script: `npm run build && electron-builder --win`)
    - C:/Users/mflma/workspace/AInotepad/electron-builder.yml (confirm the icon keys from plan 02 are present)
    - C:/Users/mflma/workspace/AInotepad/scripts/patch-wincosign.cjs (in case winCodeSign patching needs manual re-run)
  </read_first>
  <action>
    1. Clean prior dist artifacts to avoid stale icon confusion:
       `cd C:/Users/mflma/workspace/AInotepad && rm -rf dist/win-unpacked dist/Notal-*.exe dist/Notal-*.zip`
       (Keep node_modules and out/ — no need to rebuild those.)
    2. Run the full Windows build:
       `npm run build:win`
       Expected duration: 2-6 minutes on this machine. Use `run_in_background: true` if the shell would time out — orchestrator will poll for completion.
    3. On success, two artifacts appear in dist/:
       - `Notal-0.1.0-setup.exe` (NSIS installer)
       - `Notal-0.1.0-win-portable.zip` (portable ZIP)
       And `dist/win-unpacked/notal.exe` is the compiled executable.
    4. If the build fails with a winCodeSign symlink error, run `node scripts/patch-wincosign.cjs` and retry `npm run build:win`. Do NOT attempt `--no-verify` or any code-signing bypass.
    5. Extract the actual icon embedded in notal.exe via PowerShell (verifies rcedit succeeded):
       ```
       powershell -Command "Add-Type -AssemblyName System.Drawing; $icon = [System.Drawing.Icon]::ExtractAssociatedIcon('C:/Users/mflma/workspace/AInotepad/dist/win-unpacked/notal.exe'); $bmp = $icon.ToBitmap(); $bmp.Save('C:/Users/mflma/workspace/AInotepad/dist/extracted-icon.png'); Write-Host ('size=' + $icon.Width + 'x' + $icon.Height)"
       ```
    6. Read the extracted PNG via the Read tool — it should show the lemur, not a dark geometric card.
  </action>
  <verify>
    <automated>cd C:/Users/mflma/workspace/AInotepad &amp;&amp; node -e "const fs=require('fs'); const files=['dist/Notal-0.1.0-setup.exe','dist/Notal-0.1.0-win-portable.zip','dist/win-unpacked/notal.exe','dist/extracted-icon.png']; for(const f of files){ const s=fs.statSync(f); console.log(f, s.size); if(f.endsWith('.exe') &amp;&amp; s.size &lt; 1000000) throw new Error('exe too small: '+f); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:win` exits 0
    - `dist/win-unpacked/notal.exe` exists with size > 50 MB (Electron runtime + app)
    - `dist/Notal-0.1.0-setup.exe` exists with size > 100 MB (NSIS installer includes everything)
    - `dist/Notal-0.1.0-win-portable.zip` exists (portable ZIP)
    - `dist/win-unpacked/resources/app.asar.unpacked/resources/tray-icon.png` exists (asarUnpack put it there)
    - `dist/extracted-icon.png` — the icon extracted from notal.exe — when Read, shows the lemur illustration (not a dark card). Human can also inspect; automated check is "file exists, size > 500 bytes".
    - PowerShell icon-extraction step prints a size line (e.g., `size=32x32` or `size=256x256`) — any non-error output from ExtractAssociatedIcon confirms an icon is embedded.
  </acceptance_criteria>
  <done>
    Full Windows distribution built; installer + portable + unpacked exe all exist; the embedded icon in notal.exe is the illustrated lemur (confirmed via ExtractAssociatedIcon → PNG → visual read).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — install + run app, confirm illustrated icon appears everywhere</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - .planning/phases/09-app-icon/09-03-PLAN.md (this file — for the verification steps below)
  </read_first>
  <what-built>
    Plans 01+02+03 collectively: the placeholder geometric icon was replaced with the ChatGPT-generated illustrated lemur icon and wired through every Electron surface. `dist/Notal-0.1.0-setup.exe` is ready to install.
  </what-built>
  <how-to-verify>
    1. **Installer icon (Success Criterion 2):**
       - Open Windows Explorer at `C:/Users/mflma/workspace/AInotepad/dist/`
       - Confirm `Notal-0.1.0-setup.exe` shows the illustrated lemur icon (not the generic app icon, not the placeholder dark card)
       - Right-click → Properties → confirm icon in the dialog also matches

    2. **Installer wizard icon:**
       - Double-click `Notal-0.1.0-setup.exe`
       - Confirm the NSIS wizard header (top-left corner of the installer window) shows the lemur icon
       - Complete the install (install to default location)

    3. **Taskbar icon (Success Criterion 1, part 1):**
       - Launch Notal from the desktop shortcut OR `npm run dev`
       - Confirm the Windows taskbar entry (bottom of screen) shows the illustrated lemur icon
       - Hover the taskbar icon — the thumbnail preview should also show the lemur

    4. **System tray icon (Success Criterion 1, part 2):**
       - Check the system tray (bottom-right, may be in the "^" overflow)
       - Confirm Notal's tray icon is the illustrated lemur, NOT the previous 16x16 blue gradient placeholder
       - Hover — tooltip should say "Notal"
       - Right-click — menu shows Show/Hide and Quit

    5. **Size rendering (Success Criterion 3):**
       - Taskbar icon renders at ~32x32 — should look crisp, not blurry
       - Tray icon renders at 16x16 — should be recognizable (lemur silhouette visible even at this size)
       - Installer .exe icon in Explorer: switch to "Large icons" view — should render cleanly at 256x256

    6. **Uninstall path:**
       - Optional: open "Add or Remove Programs", find Notal, confirm its entry icon is also the lemur
       - The uninstaller .exe (in the install dir) should bear the illustrated icon too

    If ANY of steps 1-5 shows the old placeholder dark-card icon or a generic icon, report which step failed and the executor will diagnose (likely cache invalidation via `ie4uinit.exe -show`, or a missed key in electron-builder.yml).

    Common Windows icon-cache gotcha: Windows aggressively caches icon thumbnails. If a freshly-built .exe shows the old icon, run:
    `ie4uinit.exe -show`
    or
    `taskkill /IM explorer.exe /F && start explorer.exe`
    before re-testing.
  </how-to-verify>
  <acceptance_criteria>
    Human confirms (via `approved` or free-text description):
    - [ ] Installer .exe in dist/ shows lemur icon in Explorer
    - [ ] NSIS wizard header shows lemur icon during install
    - [ ] Taskbar entry shows lemur icon after launch
    - [ ] System tray icon shows lemur (not placeholder blue gradient)
    - [ ] All three sizes (16, 32, 256) render cleanly without obvious blur or transparency artifacts
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark Phase 09 complete. If any icon surface still shows placeholder or is broken, describe which one and the executor will issue a fix (new plan 09-04 or direct patch depending on scope).</resume-signal>
  <done>
    Human has eyeballed all four icon surfaces (installer, taskbar, tray, installed shortcut) and confirmed they show the illustrated lemur icon. Phase 09 success criteria 1, 2, and 3 are all observably met on the running system.
  </done>
</task>

</tasks>

<verification>
Combined automated (task 1) + human (task 2) verification. Task 1 produces verifiable build artifacts; Task 2 confirms Windows actually renders them. There is no way to fully verify Success Criterion 1 ("taskbar and system tray shows the custom illustrated asset") without a human eyeball — hence the checkpoint.
</verification>

<success_criteria>
- `npm run build:win` exits 0
- Installer, ZIP, and unpacked exe all exist in dist/ with expected sizes
- PowerShell-extracted icon from notal.exe, when rendered as PNG, shows the lemur
- Human verifies all four icon surfaces (installer Explorer icon, NSIS wizard header, taskbar, tray) display the illustrated lemur
- All three size targets (16, 32, 256) render cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-app-icon/09-03-SUMMARY.md` following the standard summary template. Also update `.planning/STATE.md` to mark Phase 09 complete, progress 35/35 plans complete for v1.1.
</output>
