---
phase: 01-shell-and-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - src/main/index.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/index.html
  - src/renderer/src/main.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/assets/main.css
  - src/renderer/src/components/TabBar.tsx
  - src/renderer/src/components/NotesTab.tsx
  - src/renderer/src/components/WikiTab.tsx
  - src/renderer/src/components/SearchTab.tsx
autonomous: true
requirements:
  - SYST-01
  - SYST-02

must_haves:
  truths:
    - "App launches with `npm run dev` and an Electron window opens"
    - "Window shows three tabs: Notes, Wiki, Search"
    - "Wiki and Search tabs render placeholder content (stub only)"
    - "TailwindCSS v4 styles are applied (CSS-first, no tailwind.config.ts)"
    - "better-sqlite3, sqlite-vec, and electron-updater are listed in package.json dependencies"
    - "rollupOptions.external in electron.vite.config.ts includes better-sqlite3 and sqlite-vec"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies declared"
      contains: "better-sqlite3"
    - path: "electron.vite.config.ts"
      provides: "Vite config with native module externals"
      contains: "better-sqlite3"
    - path: "src/renderer/src/App.tsx"
      provides: "Tabbed layout shell"
      contains: "Notes"
    - path: "src/renderer/src/assets/main.css"
      provides: "TailwindCSS v4 CSS-first import"
      contains: "@import"
  key_links:
    - from: "src/renderer/src/main.tsx"
      to: "src/renderer/src/App.tsx"
      via: "React render"
      pattern: "App"
    - from: "src/renderer/src/assets/main.css"
      to: "tailwindcss"
      via: "@import"
      pattern: "@import.*tailwindcss"
---

<objective>
Scaffold the AInotepad Electron application using electron-vite with React 19, TypeScript, and TailwindCSS v4. Produce a launchable app with a three-tab shell (Notes, Wiki stub, Search stub) and all Phase 1 dependencies installed.

Purpose: Every subsequent plan builds on this foundation. The scaffold determines build config, dependency tree, and code organization for all future work.
Output: Runnable Electron app, correct Vite config with native module externals, TailwindCSS v4 wired, three-tab layout shell.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold electron-vite project and install all Phase 1 dependencies</name>
  <files>package.json, electron.vite.config.ts, src/preload/index.ts, src/preload/index.d.ts</files>

  <done>All Phase 1 deps are in package.json, electron.vite.config.ts has native externals, preload bridge exposes notes API shape, @electron/rebuild is wired in postinstall.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire TailwindCSS v4 and build three-tab layout shell</name>
  <files>
    src/renderer/src/assets/main.css,
    src/renderer/src/main.tsx,
    src/renderer/src/App.tsx,
    src/renderer/src/components/TabBar.tsx,
    src/renderer/src/components/NotesTab.tsx,
    src/renderer/src/components/WikiTab.tsx,
    src/renderer/src/components/SearchTab.tsx
  </files>

  <done>App launches with Notes/Wiki/Search tabs. TailwindCSS v4 applied via CSS-first import. Wiki and Search are stubs. TypeScript compiles clean.</done>
</task>

</tasks>
