---
phase: 03-karpathy-wiki
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Submitting a note creates concept files in userData/kb/"
    - "WikiTab sidebar shows concept files grouped by tag with color indicators"
    - "Rendered Markdown view shows wikilinks as clickable links"
    - "Graph toggle shows force-directed graph with nodes colored by tag"
    - "Right-clicking a tag in sidebar opens color picker; new color persists after app reload"
    - "Back/forward navigation moves between visited concept files"
    - "Note cards in the Notes tab display colored tag indicator dots matching the tag color model"
  artifacts:
    - path: "userData/kb/"
      provides: "Concept files written by AI"
    - path: "userData/kb/_context.md"
      provides: "AI rolling context file"
---

<objective>
Human verification of the complete Phase 03 Karpathy Wiki implementation.

Purpose: All AI pipeline + WikiTab UI components are in place. This checkpoint confirms the full end-to-end flow works: note in → concept files out → wiki browsable → colors persistent → tag colors visible on note cards.

Output: Confirmed working Phase 03 implementation or issue list for gap closure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-karpathy-wiki/03-CONTEXT.md
@.planning/phases/03-karpathy-wiki/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Karpathy Wiki system:
    - AI pipeline now produces wiki_updates + tags on every note (aiWorker + aiOrchestrator)
    - Concept files written to userData/kb/ with YAML frontmatter and [[wikilinks]]
    - _context.md maintained as AI rolling memory
    - kbPages SQLite table tracks file metadata
    - WikiTab: 2-column layout (color-coded sidebar + right reading pane)
    - WikiMarkdown: react-markdown + remark-wiki-link with clickable [[wikilinks]]
    - WikiGraph: ForceGraph2D force-directed graph toggle
    - Tag color picker: right-click on sidebar tag, native color input, persists via electron-conf
    - Back/forward navigation between visited concept files
    - NoteCard: colored tag indicator dots — tag colors propagate to note annotations (locked decision)
  </what-built>
  <how-to-verify>
    **Before starting:** Kill any running Electron instance, then run `npm run dev` from C:/Users/mflma/workspace/AInotepad/

    **Test 1 — AI wiki generation:**
    1. Go to Settings tab, verify API key is configured
    2. Switch to Notes tab, submit a note with some content (e.g., "Studying quantum entanglement and its connections to TOT theory")
    3. Wait for AI processing indicator to show "complete" on the note card
    4. Open File Explorer: navigate to %APPDATA%\ainotepad\kb\ (or wherever Electron userData is)
    5. Verify at least one .md file was created (not counting _context.md)
    6. Open _context.md — verify it has YAML frontmatter (updated, note_count) and 4 sections

    **Test 2 — WikiTab sidebar:**
    1. Switch to Wiki tab
    2. Verify left sidebar shows at least one file grouped under a tag label
    3. Verify tag label has a colored square indicator next to it
    4. Verify "Knowledge Base" header appears at top of sidebar

    **Test 3 — Markdown reading pane:**
    1. Click a file in the sidebar
    2. Verify right pane shows rendered Markdown (headers, paragraphs, not raw text)
    3. If the file contains [[wikilinks]], verify they appear as colored clickable links
    4. Click a wikilink — verify it navigates to that concept file
    5. Click "←" back button — verify it returns to previous file

    **Test 4 — Graph view:**
    1. With files loaded, click "Graph" button in the pane toolbar
    2. Verify force-directed graph appears (nodes visible, at correct size — not 0×0)
    3. Verify nodes have colors matching their tag colors from the sidebar
    4. Click a node — verify it navigates to that file and switches back to Markdown view

    **Test 5 — Color picker:**
    1. Right-click a tag label in the sidebar
    2. Verify a color picker popover appears with native color input
    3. Change the color
    4. Verify the sidebar indicator dot updates to the new color
    5. Quit and relaunch the app
    6. Verify the custom color is still applied to that tag

    **Test 6 — KB updates from new notes:**
    1. Submit a second note (different topic)
    2. After AI completes, switch to Wiki tab
    3. Verify sidebar shows new/updated concept files without requiring app restart

    **Test 7 — Note card tag color indicators:**
    1. Return to the Notes tab
    2. Find a note that has been processed by the AI (aiState = "complete")
    3. Verify the note card shows small colored dot indicators for each tag assigned by the AI
    4. Verify the dot colors match the tag colors shown in the WikiTab sidebar for the same tags
    5. Change a tag color in the WikiTab sidebar (right-click → color picker)
    6. Return to Notes tab — verify the note card dots update to the new color (they refresh via kb:updated)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 tests pass.
    Type "issues: [description]" to report what failed (gap closure will be planned).
  </resume-signal>
</task>

</tasks>

<verification>
Human verification is the verification for this plan. All automated checks were completed in Plans 03-01 through 03-03.
</verification>

<success_criteria>
- All 7 verification tests pass
- User types "approved"
- Phase 03 Karpathy Wiki is complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-karpathy-wiki/03-04-SUMMARY.md`
</output>
