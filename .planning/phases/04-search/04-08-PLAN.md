---
phase: 04-search
plan: 08
type: execute
wave: 6
depends_on: [04-06, 04-07]
files_modified: []
autonomous: false
requirements: [LOCAL-01, INSIGHT-01, INSIGHT-02, INSIGHT-03, PATTERNS-01, PATTERNS-02]

must_haves:
  truths:
    - "AI pipeline runs with local model (provider=local) and produces annotated notes"
    - "Insight annotations appear inline in NoteCard in italic amber when generated"
    - "Patterns tab shows word cloud, narrative, and stats for processed notes"
    - "Brave API key (if configured) results in novelty-type insights when note contains product/idea claims"
    - "Settings panel saves local model selection and Brave key without error"
    - "App starts without ABI errors or module-not-found errors"
  artifacts:
    - path: "src/renderer/src/components/NoteCard.tsx"
      provides: "insights render verified working"
    - path: "src/renderer/src/components/PatternsTab.tsx"
      provides: "word cloud + narrative render verified working"
  key_links:
    - from: "Settings local provider selection"
      to: "note processing with local model"
      via: "end-to-end: settings save -> worker settings-update -> next note uses callLocal()"
      pattern: "local"
---

<objective>
Human verification checkpoint: confirm the full Phase 04 AI intelligence upgrade works end-to-end. Covers local model inference, inline insight display, Patterns tab rendering, and settings persistence.

Purpose: Phase 04 adds significant new infrastructure (local model, FTS5 retrieval, digest pipeline, 4 new UI sections). Human verification catches visual issues, latency problems, and integration gaps that automated checks cannot.

Output: Verification approval (or issue report for gap closure).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-search/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 04 AI intelligence upgrade:
    - node-llama-cpp integrated (Gemma 4 E4B, auto-detects hardware tier)
    - FTS5 retrieval grounds AI insights in past notes
    - Inline insight annotations in NoteCard (italic amber)
    - Brave Search API (optional, for novelty analysis)
    - Patterns tab with word cloud + AI narrative + daily/weekly toggle
    - Settings: local model provider + Brave API key field
  </what-built>
  <how-to-verify>
    Run `npm run dev` to start the app.

    **Test 1 — Basic pipeline still works:**
    Open Settings. Confirm the existing Claude or OpenAI provider still works. Submit a note. Confirm annotation appears. Confirm no console errors.

    **Test 2 — Local model provider:**
    In Settings, select "Local (Gemma 4)". Verify a hardware tier recommendation appears (small/default/large). Save settings. Submit a short note (e.g., "Ideas for a new todo app that uses AI to prioritize tasks"). Wait for AI processing (may take 30-120 seconds on CPU). Confirm annotation appears. If an insight is generated, confirm it appears in italic amber beneath the annotation.

    **Test 3 — Insight display:**
    Submit several notes referencing related topics (e.g., "I keep thinking about quantum mechanics", "The observer effect in quantum physics is fascinating"). After processing, check if an insight appears connecting the notes. Verify: italic text, amber/gold color (not blue), appears below the blue annotation.

    **Test 4 — Patterns tab:**
    Click the "Patterns" tab. Verify label says "Patterns" not "Search". If notes have been processed with AI, verify word cloud appears with topic tags. Verify AI narrative appears below the word cloud. Verify daily/weekly toggle buttons work. If no digest yet, verify the empty state message shows.

    **Test 5 — Settings:**
    Open Settings. Verify Brave API key field is present (labeled "optional"). Verify the 4 provider radios: Ollama, Claude, OpenAI, Local (Gemma 4). Verify saving without errors.

    **Test 6 — App startup:**
    Close and reopen the app. Verify no ABI mismatch errors or module-not-found errors in the terminal output or DevTools console.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all tests pass.
    Or describe specific issues for gap closure (include which test failed and the exact error/symptom).
  </resume-signal>
</task>

</tasks>

<verification>
Human verification — see task above.
</verification>

<success_criteria>
- Local model loads and processes notes without crashing
- Insights appear inline when generated (correct styling)
- Patterns tab renders digest content (or empty state)
- Settings panel works with all 4 providers
- No console errors on startup or normal use
</success_criteria>

<output>
After verification, create `.planning/phases/04-search/04-08-SUMMARY.md` with verification results.
</output>
